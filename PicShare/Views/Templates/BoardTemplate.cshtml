@{
    Layout = null;
}
<div class="row text-center">
    <div class="col-md-2 col-md-offset-5">
        <h3 data-bind="text: userName" />
    </div>
</div>

<div class="row">
    <button class="col-md-2 col-md-offset-1 pull-right btn btn-info" data-bind="click: addNewPicture">Add new picture</button>
    <button class="col-md-2 pull-right btn btn-info" data-bind="click: goToUserDetail">Visit your own page</button>
</div>

<!-- ko if: isUploadFormVisible -->
<file-uploader params="{imgUploaded: imgUploaded, userId: userId}"></file-uploader>
<!-- /ko -->

<div class="row">
    board
</div>